# Dépendances de l'entrepôt, séparées de l'API (venv hanabi-dwh/.venv).

# dbt-core plafonné à 1.11 par dagster-dbt
dbt-core==1.11.*
dbt-postgres==1.11.*

# psycopg 3 pour ingestion/ (dbt-postgres embarque psycopg2)
psycopg[binary]==3.2.*

# dagster-dbt épingle dagster : les versions se mettent à jour ensemble
dagster==1.13.*
dagster-dbt==0.29.*

# Interface web de Dagster, pour lire le graphe et relancer des partitions
dagster-webserver==1.13.*
